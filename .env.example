# OpenAI API Configuration (Required)
# Get your API key from: https://platform.openai.com/api-keys
# Sign up at: https://platform.openai.com/signup
OPENAI_API_KEY=sk-proj-your_actual_api_key_here

# AI Model Selection (Required)
# Available options: gpt-4o-mini, gpt-4o, gpt-4-turbo
# Recommended: gpt-4o-mini (fastest and cheapest, suitable for this application)
MODEL_NAME=gpt-4o-mini

# Agent Directory Paths (Optional - defaults work for standard setup)
# These are relative paths from the run_agent/ directory
PROMPTS_DIR=./prompts
STRATEGIES_DIR=./strategies
GAME_STATE_PATH=./game_state

# Performance Tuning (Optional)
# Only change these if you experience performance issues

# GPU Memory: Fraction of GPU VRAM to allocate for PaddleOCR
# - Default: 0.5 (50% - works for most systems)
# - Low-end GPUs with <2GB VRAM: Set to 0.3 (30%)
# - High-end GPUs with 6GB+ VRAM: Can increase to 0.8 (80%)
# - If you get OOM (Out of Memory) errors, reduce this value
PADDLE_GPU_MEMORY_FRACTION=0.5

# OCR Scale Testing: Number of image scales to test per OCR operation
# - Default: 5 (best accuracy, tests 5 different zoom levels)
# - Fast mode: 3 (about 40% faster, slight accuracy tradeoff)
# - Use fast mode (3) if OCR processing takes too long on your system
OCR_MAX_SCALES=5

# ==============================================================================
# SETUP INSTRUCTIONS
# ==============================================================================
#
# 1. Copy this file to create your actual .env file:
#    copy .env.example .env
#
# 2. Edit the .env file and replace 'your_actual_api_key_here' with your
#    actual OpenAI API key
#
# 3. The .env file is already in .gitignore - never commit API keys to git!
#
# 4. For more information on environment variables, see the README.md file
#
# ==============================================================================
