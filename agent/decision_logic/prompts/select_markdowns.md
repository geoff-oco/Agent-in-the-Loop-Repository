Select the most relevant strategy markdown file for the given game state and global context, each contains a specified strategy relevant to different situations.
You MUST call the tool `load\_markdowns` with an argument `name` that contains a string value of ONLY 1 filename,
chosen ONLY from the provided list of strategy filenames.
Do NOT output any prose—just perform the tool call.



